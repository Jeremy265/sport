const config = {
    basename: '',
    defaultPath: '/',
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 12,
    gridSpacing: 2,
    drawerWidth: 260,
    messageDuration: 3000
}

export default config